console.log('Hello Word');

//Árvóre de natal em ASCII
const NUM_LINHAS=10;

for(let i=0; i<NUM_LINHAS; i++){
    let espacos = ' '.repeat(NUM_LINHAS-i);
    let conteudo = 'X'.repeat(i);
    console.log(espacos + conteudo + 'X' + );
}

console.log(''.repeat(NUM_LINHAS)+ 'X' + '\n').repeat(4);